name: Humble Binary Build Main
on:
  workflow_dispatch:
  pull_request:
    branches:
      - humble
  push:
    branches:
      - humble
  schedule:
    - cron: '53 5 * * *'

jobs:
  humble_binary_main:
    uses: ./.github/workflows/reusable_ici.yml
    with:
      ros_distro: humble
      ros_repo: main
      ref_for_scheduled_build: humble
